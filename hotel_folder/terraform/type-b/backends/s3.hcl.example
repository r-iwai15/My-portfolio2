# Copy to s3.hcl and run:
#   terraform init -reconfigure -backend-config=backends/s3.hcl
bucket       = "YOUR-TERRAFORM-STATE-BUCKET"
key          = "hotel-innovative/terraform.tfstate"
region       = "ap-northeast-1"
encrypt      = true
use_lockfile = true
