# ポートフォリオ説明書（自分用）

> **目的:** このフォルダ（`ポートフォリオのコピー5`）を自分が把握しきれていなくても、面接・社内説明・README 案内で**人に話せる**ようにするためのガイドです。  
> **更新目安:** 大きな構成変更があったら `hotel_folder/STRUCTURE.md` と照合してください。

---

## 0. まず覚える3行（30秒ピッチ）

1. **本番 SaaS ではない。** クラウドセキュリティ・IaC・AI エージェント設計を**比較検証したポートフォリオ**。
2. **中心は `hotel_folder/`。** 同じ「ホテル予約」を **Type-B（伝統型）** と **Type-A（サーバーレス型）** の2通りで設計し、Terraform + テスト付き Lambda で示している。
3. **周辺にログ調査（Athena）とオフライン PCAP 監査** があり、「設計だけでなく調査・ガバナンスもできる」ことを補強している。

---

## 1. フォルダ全体マップ

```
ポートフォリオのコピー5/
├── README.md                 … 対外用の顔（英語版 README.en.md あり）
├── 説明書_自分用.md           … 本ファイル
│
├── hotel_folder/             … ★ メイン。ここだけ深く知れば7割話せる
│   ├── lambda/               … Type-A / Type-B のソース + ビルド ZIP
│   ├── terraform/            … 外向け AWS 構成（type-a, type-b, corp-internal）
│   ├── internal-infra/       … 社内向けの Type-A / Type-B 比較（別テーマ）
│   ├── packages/             … 予約 JSON Schema など共有定義
│   ├── docs/                 … 設計書・手順書
│   ├── frontend/             … デモ用スタブ（本番 UI ではない）
│   └── Makefile              … test / build / terraform 検証
│
├── AWS_log/                  … CloudTrail / VPC Flow を Athena で調べる SQL 例
└── analysis_wireshark/       … オフラインで Egress 通信を PCAP 解析
```

| フォルダ | 何を見せたいか | 話すときのキーワード |
|----------|----------------|----------------------|
| `hotel_folder/` | アーキテクチャ比較・IaC・AI 関所 | Type-B vs Type-A、Sentinel、Bedrock Agent |
| `AWS_log/` | ログからの脅威ハンティング | Athena、CloudTrail、VPC Flow |
| `analysis_wireshark/` | データ主権・オフライン監査 | PCAP、GeoLite、Egress |

---

## 2. これは何を「証明」しているポートフォリオか

面接官・技術者に伝えたいのは次の4点です（全部を一度に言う必要はありません）。

| # | 伝えたいこと | 根拠になる場所 |
|---|--------------|----------------|
| 1 | **ビジネス文脈でインフラを分けられる** | Type-B / Type-A の対比、`docs/DESIGN_TYPE_B_VS_A.md` |
| 2 | **IaC を読みやすく書ける** | `terraform/` のファイル分割・子モジュール（VPC / Sentinel） |
| 3 | **AI を前面に出しすぎない設計ができる** | Type-A の interface / reader / reservation 分離、Sentinel（ルール実行） |
| 4 | **運用・調査の視点もある** | `AWS_log/`、`analysis_wireshark/`、CloudTrail / Flow Logs の Terraform |

**意図的にやっていないこと（正直に言えると信頼される）:**

- 公開デモ URL・本番 AWS への常時デプロイ
- Stripe 本番決済・OTA 全チャネル連携
- フル機能フロントエンド

→ 詳細: `hotel_folder/docs/PORTFOLIO_STRATEGY.md`

---

## 3. `hotel_folder` の心臓部

### 3.1 2つの「ホテル向け」外向けアーキテクチャ

同じドメイン（ホテル予約）を、**顧客・ブランドが違うとインフラ思想も違う**というストーリーで並べています。

| | Type-B（Ultimate Edition） | Type-A（Ultimate Security Edition） |
|---|---------------------------|-------------------------------------|
| **想定ホテル** | 歴史ある高級ホテル。既存システムを大きく変えたくない | インバウンド・デジタルネイティブ向けブティック |
| **一言** | 表は従来型、**裏で AI が守る** | **会話が入口**のサーバーレス |
| **技術の骨格** | VPC + ALB + RDS + EC2(LLM) + CloudFront | API GW + Lambda + DynamoDB + Bedrock Agent |
| **AI の役割** | ゲスト向けチャットは補助。**Sentinel** がログから攻撃を検知し WAF / Cognito を操作 | Bedrock Agent がオーケストレーション。ただし**書き込みは execution 層** |
| **Terraform** | `terraform/type-b/` | `terraform/type-a/` |
| **Lambda ソース** | `lambda/type-b/` | `lambda/type-a/` |

### 3.2 Type-A の「関所」モデル（よく聞かれるので）

AI に全部やらせないため、Lambda を役割で分けています。

```
ゲスト
  → API Gateway
    → interface（対話・Agent 呼び出し。副作用なし）
         → Bedrock Agent
              → tools/reader（DynamoDB 読み取りだけ）
              → execution/reservation（検証済み JSON だけ書き込み）
```

| パス | 名前の由来 | 実際の仕事 |
|------|-----------|-----------|
| `lambda/type-a/interface/` | 窓口 | リクエストを受け、Agent を呼ぶ |
| `lambda/type-a/tools/reader/` | 読取専用 Tool | 予約情報の参照 |
| `lambda/type-a/execution/reservation/` | 実行層 | AJV でスキーマ検証 → DynamoDB 書き込み（冪等キーあり） |

スキーマ: `packages/reservation-schema/`  
**まだ理想形ではない点:** Agent が「いつ」 execution を呼ぶかは AI 判断に一部依存。完全決定論にするなら「Agent は JSON だけ返す」へ寄せる、と説明できるとよい。

### 3.3 Type-B の Sentinel（Sentinel = 番人）

| コンポーネント | 役割 |
|----------------|------|
| `lambda/type-b/chatbot/` | 会話 UI 用。VPC 内 LLM（vLLM）へプロキシ |
| `lambda/type-b/sentinel_executioner/` | チャットログの**危険パターン**検知 → WAF IP セット更新・Cognito 無効化 |
| `lambda/type-b/sentinel_release/` | 一定時間後にブロック解除（TTL） |
| `terraform/modules/sentinel/` | 上記をまとめた Terraform 子モジュール |
| `terraform/type-b/waf_cloudfront.tf` | WAF IP セット（Sentinel が触る） |

**ポイント:** Sentinel は **LLM ではなくルール＋Lambda**。「AI ホテル」と言いつつ、防衛は決定論的、と話すと設計意図が伝わります。

### 3.4 社内向け `internal-infra/`（外向けとは別テーマ）

外向けホテルと**同じ Type-A / Type-B というラベル**を、社内 IT に当てはめた比較です。

| | 社内 Type-B | 社内 Type-A |
|---|------------|-------------|
| 想定 | 大手・既存 AD・オンプレあり | スタートアップ・フルリモート |
| キーワード | ハイブリッド、変更管理、検知型 | ZTNA、Verified Access、予防型 |
| コード | `internal-infra/terraform/type-b` など | `internal-infra/terraform/type-a` など |

**話の分け方:** 「外向けはホスピタリティ、内向けは組織文化」と一言で切る。

### 3.5 Terraform の読み方（自分用チート）

| パス | 内容 |
|------|------|
| `terraform/type-b/` | 外向け・伝統型スタック（ファイル分割済み + `module.vpc` + `module.sentinel`） |
| `terraform/type-a/` | 外向け・サーバーレススタック（`module.vpc` = プライベート VPC） |
| `terraform/corp-internal/` | 社内最小 VPCE スタック（別用途） |
| `terraform/modules/` | 共通子モジュール（`vpc_type_b`, `vpc_type_a`, `sentinel`, `cloudwatch_log_group`） |

**State の考え方:**

- 子モジュール（VPC / Sentinel）も **親 stack と同じ state**（別 state にはしていない）。
- ローカル検証: backend は `local`（`.state/terraform.tfstate`）。
- 本番比較: S3 backend に切替 → `docs/TERRAFORM_PLAN.md` / `terraform/*/backends/README.md`。

**リファクタの痕跡:** `moved.tf` は、以前 root にあったリソースを子モジュールへ移したとき **state を壊さない**ためのもの。面接で聞かれたら「module 化時に moved を入れた」と言えば十分。

---

## 4. 周辺2フォルダ（短く話す用）

### 4.1 `AWS_log/`

- **何:** CloudTrail / VPC Flow Logs を **Athena で SQL 分析**する例。
- **何ではない:** Terraform でインフラを立てるプロジェクトではない。
- **話す例:** 「API 誰が・いつ・何をしたか」「REJECT された Flow を追う」など、SOC 的な調査手順のサンプル。

### 4.2 `analysis_wireshark/`

- **何:** PCAP から **外向き通信（Egress）だけ**を抽出し、GeoLite で国・ASN を**完全オフライン**付与。
- **なぜあるか:** ローカル LLM が勝手に外部送信していないか、ログをクラウドに上げずに検証する、という **データ主権**の話。
- **入口:** `analyze_ai_traffic.py`（GeoLite は `geolite/` 配下を自動検出する想定）。

---

## 5. 動かし方・信頼できると言えること

`hotel_folder/` で:

```bash
cd hotel_folder
make test              # 全 Lambda テスト
make ci                # test + terraform fmt/validate
make build             # lambda/*.zip を生成
make terraform-plan-type-b   # ローカル state で plan（AWS 認証があるとより現実的）
```

**今の状態（2026年5月時点）:**

- `make test` / `make terraform-validate` は通る想定。
- `terraform plan` はローカル backend で実行可能（空 state なら「全部 create」の plan）。
- **AWS 本番アカウントへの apply は未実施**（ポートフォリオ方針）。

CI: ルート `.github/workflows/ci.yml` が `make ci` 相当を回す構成。

---

## 6. 説明シナリオ別テンプレ

### 6.1 1分版（エレベータ）

「ホテル予約を題材に、格式重視の Type-B とアジャイルな Type-A の AWS 設計を Terraform で並べました。Type-A は AI に予約を書かせず実行層で検証、Type-B は Sentinel でプロンプトインジェクションをルール検知します。周辺に Athena ログ調査とオフライン PCAP 監査もあり、設計から運用調査までを一つのポートフォリオにしています。」

### 6.2 5分版（技術面接）

1. 問題設定: 同じ業務でもブランドでインフラが変わる  
2. Type-B: 3層維持 + Sentinel + WAF（`terraform/type-b`）  
3. Type-A: Bedrock Agent + 関所 Lambda（`interface` / `reader` / `reservation`）  
4. 品質: `make test`、Schema + AJV、冪等キー、Terraform モジュール化  
5. スコープ外を明示: 本番 URL・決済・フル UI は意図的に未実装  

### 6.3 深掘りされたとき

| 質問 | 答えの方向 | 見るファイル |
|------|-----------|--------------|
| Type-B と Type-A の違いは？ | 上表 + ブランドストーリー | `docs/DESIGN_TYPE_B_VS_A.md` |
| AI をどう封じ込めた？ | 実行層・Sentinel・スキーマ | `execution/reservation/`, `modules/sentinel/` |
| Terraform どう整理した？ | ファイル分割 + VPC/Sentinel 子モジュール | `docs/TERRAFORM_MODULARIZATION.md` |
| 社内版は？ | `internal-infra/` は別テーマ | `internal-infra/README.md` |
| 図は？ | Mermaid | `docs/ARCHITECTURE.md` |

---

## 7. 用語ミニ辞典

| 用語 | このポートフォリオでの意味 |
|------|---------------------------|
| **Type-A / Type-B** | 2つの対照的な設計パターン名（外向け・社内の両方でラベル再利用） |
| **Sentinel** | Type-B の自動防御 Lambda 群（旧 OpenClaw 相当の位置づけ） |
| **関所** | AI と DB の間に入る検証・分離レイヤー |
| **Gray ゾーン** | AI が関与するが、書き込みはスキーマ検証を通す領域 |
| **corp-internal** | 社内 VPCE など最小構成の Terraform（ホテル本線とは別） |
| **スタブ** | 動くサンプル実装。本番完成品ではない |

---

## 8. ドキュメント索引（迷ったらここ）

| 読みたいこと | ファイル |
|--------------|----------|
| 対外用概要 | ルート `README.md` / `README.en.md` |
| スコープ・やってないこと | `hotel_folder/docs/PORTFOLIO_STRATEGY.md` |
| フォルダ一覧 | `hotel_folder/STRUCTURE.md` |
| Type 比較の設計思想 | `hotel_folder/docs/DESIGN_TYPE_B_VS_A.md` |
| 技術スタック一覧 | `hotel_folder/docs/TECHNICAL_OVERVIEW.md` |
| 手順 | `hotel_folder/docs/IMPLEMENTATION_GUIDE.md` |
| アーキテクチャ図 | `hotel_folder/docs/ARCHITECTURE.md` |
| Terraform plan | `hotel_folder/docs/TERRAFORM_PLAN.md` |
| 実ホテル連携の話（参考） | `hotel_folder/docs/REAL_WORLD_HOTEL_INTEGRATIONS.md` |
| AI エージェント設計ガイドライン | ユーザーホームの `AIエージェント設計ガイドライン：〜.md`（リポ外） |

---

## 9. よくあるつまずき（自分向け FAQ）

**Q. どこから見ればいい？**  
A. `hotel_folder/STRUCTURE.md` → `docs/ARCHITECTURE.md` → `make ci` が通ることを確認。

**Q. Type-Omega / OpenClaw という名前は？**  
A. リネーム済み。**Type-A** と **Sentinel**。古い資料があったらこの名前で話す。

**Q. `terraform:` みたいなフォルダは？**  
A. 旧構成。今は `hotel_folder/terraform/`。

**Q. 本番にデプロイしてデモできる？**  
A. ポートフォリオ方針で未ホスト。話すときは「IaC とテストで再現可能」と言う。

**Q. 子モジュール化で state は壊れない？**  
A. `moved.tf` を入れている。既存 AWS に apply 済みの環境では初回 plan を必ず目視。

**Q. 全部理解しないとダメ？**  
A. いいえ。**§0 の3行 + §3.1 の表 + `make ci`** だけでも十分に「説明する側」になれる。深い質問は §8 の索引で該当 md を開けばよい。

---

## 10. 面接対策

Q&A 100問超・自己紹介・逆質問・STAR 例: [`面接対策_自分用.md`](面接対策_自分用.md)

---

## 11. チェックリスト（説明前の5分準備）

- [ ] `cd hotel_folder && make ci` が通る
- [ ] Type-B / Type-A の違いを一言ずつ言える
- [ ] Sentinel が「ルール実行」であることを言える
- [ ] 本番 SaaS ではないことを最初に言える
- [ ] 聞かれたら `docs/ARCHITECTURE.md` を画面共有できる

---

*この説明書はリポジトリ構成に合わせて書かれています。大きなリネームやフォルダ移動があった場合は、`STRUCTURE.md` と突き合わせて更新してください。*
