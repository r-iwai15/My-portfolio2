# 面接対策（自分用・Q&A 大全）

> 前提: 新卒〜ジュニア向け。**全部暗記不要。** 印（★）は優先度高。  
> 地図は [`説明書_自分用.md`](説明書_自分用.md)、図は [`hotel_folder/docs/ARCHITECTURE.md`](hotel_folder/docs/ARCHITECTURE.md)。

---

## 使い方

1. **★だけ** 最初に読む（§2 自己紹介 + §4 の★10問）
2. 志望が **インフラ / セキュリティ / アプリ** のどれかで §5〜§7 を重点的に
3. 直前は §12 チェックリスト

**話すときの鉄則:** 本番 SaaS ではない → 主戦場を1つ → わからないは正直に。

---

## 1. 自己紹介テンプレ

### 30秒（★）

「新卒の〇〇です。ポートフォリオでは、ホテル予約を題材に、格式重視の Type-B とアジャイルな Type-A の2系統を AWS と Terraform で比較しました。Type-A は AI に DB を直接触らせず実行層で検証、Type-B は Sentinel でプロンプトインジェクションをルール検知します。本番サービスではなく設計・IaC・テストの検証が目的で、周辺に Athena ログ調査とオフライン PCAP 監査も入れています。」

### 1分（インフラ志望）

上に加えて:  
「Terraform は type-a / type-b をファイル分割し、VPC と Sentinel を子モジュール化しました。`make ci` で Lambda テストと terraform validate まで通す構成です。デプロイ済みの公開デモはなく、ローカル plan とテストで再現性を担保しています。」

### 1分（セキュリティ志望）

上に加えて:  
「防御の例として、Type-B の Sentinel は CloudWatch Logs のサブスクリプションフィルターで危険文言を検知し、WAF IP セットと Cognito 無効化を行います。Type-A 側は予約 JSON を AJV で検証し、冪等キーで二重予約を抑止する execution 層を分けています。」

### 3分（深掘り用・最後に主戦場）

1. 課題意識（同じ業務でもブランドでインフラが変わる）  
2. Type-B 概要（3層 + Sentinel）  
3. Type-A 概要（Agent + 関所）  
4. 自分が一番触った所（**ここを具体化。例: reservation のテスト、Sentinel のコード読解**）  
5. スコープ外（決済本番、デモ URL）と今後やりたいこと  

---

## 2. 必ず言えるキーワード（チートシート）

| 用語 | 一言 |
|------|------|
| Type-B | 伝統3層 + AI は裏方（Sentinel） |
| Type-A | サーバーレス + Bedrock Agent + 関所分離 |
| Sentinel | ログのルール検知 → WAF / Cognito（LLM ではない） |
| interface | 対話の窓口。副作用なし |
| execution/reservation | スキーマ検証・料金・冪等のあと DynamoDB 書込 |
| Gray ゾーン | AI が関与するが、書込は決定論的ゲートを通す領域 |
| 関所 | AI と永続化の間の検証レイヤー |
| corp-internal | 社内 VPCE など最小スタック（本線は internal-infra） |
| ポートフォリオスコープ | 設計比較。本番 SaaS 完成品ではない |

---

## 3. 総合・ポートフォリオ全般

### Q1. このポートフォリオは何ですか？（★）

**A.** クラウド・セキュリティ・AI エージェント設計を**比較検証**した作品集。中心は `hotel_folder/` の Type-B / Type-A。周辺に Athena ログ調査とオフライン PCAP 監査がある。本番ホテル SaaS ではない。

### Q2. なぜホテル？

**A.** 予約・本人確認・ログ・外部連携など、**セキュリティと UX のトレードオフ**が説明しやすいドメインだから。製造・金融でもよかったが、ストーリーが伝わりやすい。

### Q3. 一人で全部作りましたか？

**A.** （事実に合わせて）設計・実装・ドキュメントの主体は自分。□□□は参考・共同、と正直に。新卒で「全部一人」は必須ではない。

### Q4. 本番運用していますか？（★）

**A.** していない。公開デモ URL もない。`make test` / `terraform validate` / ローカル `plan` で検証。本番では state・監視・コスト・障害対応が別途必要。

### Q5. 一番得意な部分は？

**A.** （自分で埋める）例: Type-A の execution 層とテスト / Type-B の Sentinel の流れ / Terraform のモジュール構成。

### Q6. 一番苦労した点は？

**A.** 例: Type-A で「AI に何をさせないか」を関所で表現したこと / 巨大 Terraform を分割・モジュール化したこと / スコープを「完成品」に広げすぎない判断。

### Q7. なぜ Type を2つ？

**A.** **顧客・ブランドが違うと最適解が違う**ことを示すため。高級ホテルは既存資産維持（Type-B）、デジタルネイティブ向けは速度とサーバーレス（Type-A）。

### Q8. 社内版（internal-infra）とは？

**A.** 外向けホテルと**同じラベル**で、社内 IT（統制型 vs クラウドネイティブ）を比較した別テーマ。話を混ぜない。

### Q9. AWS_log / analysis_wireshark は何のため？

**A.** 設計だけでなく**運用・調査**の視点。Athena で API/Flow を追う例、PCAP でオフライン Egress 監査の例。

### Q10. このポートフォリオで証明したいことは？

**A.** ①文脈でアーキを分けられる ②IaC を読みやすく書ける ③AI を前面に出しすぎない ④ログ・調査の入口がある。

### Q11. 完成度は何％？

**A.** 設計・IaC・テスト付きスタブとしては一定完成。本番としては決済・OTA・フル UI・DR・コスト最適化などが未着手。`PORTFOLIO_STRATEGY.md` に境界を書いた。

### Q12. なぜ GitHub に出せる？

**A.** シークレットをコミットしていない。バックエンドはプレースホルダ。本番アカウント ID やキーは含めない方針。

### Q13. CI は？

**A.** `.github/workflows/ci.yml` で `make ci`（テスト + terraform fmt/validate）相当。

### Q14. 読む順番は？

**A.** `STRUCTURE.md` → `ARCHITECTURE.md` → `make ci` → 興味のある Terraform / Lambda。

### Q15. 新卒でここまでやる意味は？

**A.** チュートリアル超えの**設計の幅**と、テスト・IaC・セキュリティの接続を早めに示すため。入社後はチームの規模・規約に合わせて深める。

---

## 4. Type-B vs Type-A 比較

### Q16. Type-B と Type-A の違いを一言で（★）

**A.** Type-B は**表は従来型・裏で AI 防御**。Type-A は**会話が入口のサーバーレス**で、書込は execution 関所。

### Q17. どちらが「正解」？

**A.** どちらも正解ではない。**制約（ブランド・既存資産・規制）**で選ぶ。比較がポートフォリオの主題。

### Q18. Type-B 向きの組織は？

**A.** 既存 RDS/基幹を変えたくない、変更説明責任が重い、格式・会員基盤が強い。

### Q19. Type-A 向きの組織は？

**A.** デジタルネイティブ、多言語・チャット UX、サーバ運用を抑えたい。

### Q20. データストアの違いは？

**A.** Type-B は **RDS（MySQL）** が中心。Type-A は **DynamoDB**。

### Q21. AI の置き方の違いは？（★）

**A.** Type-B: ゲスト向けは chatbot 補助、**防御は Sentinel（ルール）**。Type-A: **Bedrock Agent** がオーケストレーション、ただし予約書込は execution。

### Q22. ネットワークの違いは？

**A.** Type-B: 公開/アプリ/DB/Lambda/LLM サブネットなど**多層 VPC + NAT**。Type-A: **プライベートサブネット + VPC エンドポイント**中心。

### Q23. フロントの違いは？

**A.** どちらも CloudFront + Cognito 系の構成要素はあるが、Type-B は ALB 経由の従来 Web 寄り、Type-A は API + 静的配信寄り。`frontend/` はスタブ。

### Q24. コスト感は？

**A.** Type-B は NAT・RDS・EC2(LLM) で固定費が出やすい。Type-A は従量だが Bedrock/Lambda/DynamoDB の設計次第。本番試算はしていないと正直に。

### Q25. 移行はどう考える？

**A.** Type-B → A は**段階的**（読取 API、新規チャネルだけサーバーレス等）。一括リプレイスはリスク大、と説明できるとよい。

---

## 5. Type-A 深掘り

### Q26. Type-A のリクエストフローは？（★）

**A.** ゲスト → API Gateway → **interface** → Bedrock Agent →（reader で読取 / execution で書込）→ DynamoDB。

### Q27. interface の責務は？

**A.** 認証後リクエストを受け、**Agent を呼ぶだけ**。予約確定などの副作用は持たない。

### Q28. tools/reader の責務は？

**A.** DynamoDB の**読み取り専用** Tool。Agent から呼ばれる。

### Q29. execution/reservation の責務は？（★）

**A.** 構造化された予約意図を **AJV（JSON Schema）で検証** → **pricing-engine で料金計算** → **idempotencyKey で冪等** → DynamoDB 書込。LLM は使わない。

### Q30. なぜ Agent に直接 DynamoDB 書かせない？

**A.** 幻覚・プロンプトインジェクション・二重予約・料金改ざんを防ぐ。**決定論的関所**が必要。

### Q31. Gray ゾーンとは？

**A.** AI が「何をするか」に関与するが、**永続化の直前はコードとスキーマ**で縛る領域。完全自動任せ（Black 直結）ではない。

### Q32. 理想形と現状のギャップは？（★・正直回答）

**A.** 理想は Agent が**構造化 JSON だけ**返し、実行タイミングは常に orchestrator/execution が決める。現状は Agent が Tool として execution を呼ぶタイミングに **AI 判断が残る**。スキーマ・AJV は第一段の関所。

### Q33. JSON Schema はどこ？

**A.** `packages/reservation-schema/` と execution 内の `intent.schema.json`。

### Q34. 冪等キーはなぜ必要？

**A.** ネットワーク再送・Agent の再試行で**二重予約**が起きうるため。同じキーなら同じ結果に寄せる。

### Q35. pricing-engine を Lambda 外にした理由は？

**A.** 料金ロジックを**テスト可能な純関数**に近づけ、LLM と分離するため（`packages/pricing-engine`）。

### Q36. Bedrock Agent の役割は？

**A.** 対話と Tool 呼び分けの**オーケストレーター**。ビジネスルールの最終確定は execution。

### Q37. VPC エンドポイントを張る理由は？

**A.** Lambda から AWS API へ**プライベート経路**で出す。データ経路とガバナンス（Flow Logs、KMS）のため。

### Q38. Type-A のセキュリティ強化ポイントは？

**A.** Cognito、KMS、CloudTrail/Config/GuardDuty、WAF（構成による）、最小権限 IAM、VPC エンドポイント。

### Q39. Lambda を3つに分けた IAM の意味は？

**A.** interface / reader / reservation で**権限を分離**。reader は書込不可、reservation だけ Put など。

### Q40. テストは何をしている？

**A.** `make test` で interface / reader / reservation など Vitest/pytest。 reservation はスキーマ・冪等・料金など複数ケース。

### Q41. Type-A で「サーバーレス」のメリット・デメリットは？

**A.** メリット: 運用スケール、機能単位の分離。デメリット: デバッグ・レイテンシ・Bedrock 依存・IAM/VPC の複雑さ。

### Q42. DynamoDB のキー設計を説明できる？

**A.** （読んでいれば）PK/SK と GSI（guestName 等）。読んでいなければ「予約検索用に GSI を定義。詳細は storage.tf」と正直に。

### Q43. API Gateway の認証は？

**A.** Cognito JWT 想定（authorizer）。ゲスト向けチャネルのトークン検証。

### Q44. CloudFront の役割は？

**A.** 静的コンテンツ配信、エッジ、WAF 連携（Type-A の cloudfront.tf 参照）。

### Q45. もし Type-A を本番化するなら最初に何をする？

**A.** ①Agent 出力を JSON のみに制限 ②本番 state・監視・アラート ③コスト上限 ④ペネトレーション・脅威モデリング ⑤決済は別システム連携。

---

## 6. Type-B 深掘り

### Q46. Type-B の全体像は？（★）

**A.** CloudFront → ALB → 従来アプリ層 + **RDS**。チャットは **Lambda（chatbot）→ VPC 内 vLLM**。防御は **Sentinel** がログから検知。

### Q47. なぜ RDS を残す？

**A.** 既存ホテル・監査・レポート・JOIN など**relational な運用**に合わせるストーリー。Type-B の「変えない」思想。

### Q48. LLM はどこで動く？

**A.** **EC2（GPU）サブネット**上の vLLM を chatbot Lambda が HTTP で呼ぶ構成（bedrock_api.tf 周辺の設計）。

### Q49. Bedrock は Type-B で使う？

**A.** ゲスト向け主役は **VPC 内 LLM**。Bedrock というより自前 LLM エンドポイント寄り（コード上 LLM_ENDPOINT）。

### Q50. Sentinel とは？（★）

**A.** **番人**。CloudWatch Logs で危険パターン（例: プロンプトインジェクション文言）を検知し、**WAF IP セット更新**と **Cognito ユーザー無効化**、DynamoDB にブロック記録。

### Q51. Sentinel は AI？

**A.** **いいえ。ルールベースの Lambda**（Python）。ログフィルタパターンでトリガー。

### Q52. sentinel_executioner と sentinel_release の違いは？

**A.** executioner: **ブロック実行**。release: **TTL 後に IP 解放**（定期 EventBridge）。

### Q53. なぜログから検知？

**A.** チャットボットへの入力は外部攻撃面。アプリ前段で全部防げないので**事後検知＋封じ込め**。

### Q54. WAF のスコープは？

**A.** CloudFront 用 IP セット（`SCOPE=CLOUDFRONT`、リージョン us-east-1 等）。コードの env を参照。

### Q55. ブロック追跡テーブルの意味は？

**A.** どの IP をいつブロックしたか。**TTL** と release で運用可能に。

### Q56. Type-B の chatbot のリスクは？

**A.** プロンプトインジェクション、過剰権限、ログへの PII。だから Sentinel と WAF。

### Q57. NAT ゲートウェイはなぜ1台構成？

**A.** コスト最適化のコメントあり（network モジュール）。本番は AZ 冗長化が論点。

### Q58. メール送信 Lambda は？

**A.** messaging.tf 周辺。予約確認等の通知想定（スタブ ZIP）。

### Q59. Type-B の監査系は？

**A.** CloudTrail、Flow Logs、KMS 暗号化、Config、GuardDuty 等（monitoring / 各 tf）。

### Q60. ALB の前に CloudFront の理由は？

**A.** エッジ、WAF、キャッシュ、オリジン保護。

### Q61. セキュリティグループの考え方は？

**A.** CloudFront 由来のみ ALB、層間は必要ポートのみ、DB はアプリからのみ等（security_groups.tf）。

### Q62. Type-B を本番化するなら？

**A.** Multi-AZ NAT、RDS バックアップ検証、WAF チューニング、Sentinel の誤検知調整、秘密情報管理、負荷試験。

---

## 7. Terraform / IaC

### Q63. なぜ Terraform？

**A.** 再現性、レビュー可能性、モジュール化、AWS リソースの宣言的管理。

### Q64. main.tf を分割した理由は？（★）

**A.** レビューと責務の明確化。巨大1ファイルは差分が読めない。

### Q65. 子モジュール vpc / sentinel の意味は？

**A.** ネットワークと防御を**境界として切り出し**。state は親と同一（別 state にはしていない）。

### Q66. moved.tf とは？

**A.** リソースを module に移したとき **state アドレスを移行**し、destroy/recreate を避けるため。

### Q67. backend は？

**A.** ローカル検証は `local`（`.state/`）。本番想定は S3（`backends/s3.hcl`）。`TERRAFORM_PLAN.md` 参照。

### Q68. terraform validate と plan の違いは？

**A.** validate は構文・参照チェック。plan は AWS との差分（要 init/認証）。

### Q69. モジュールとスタックの違いは？

**A.** スタック = type-a / type-b の root。モジュール = 再利用部品（vpc_type_b, sentinel 等）。

### Q70. corp-internal とは？

**A.** 社内向け**最小 VPCE** などの別スタック。ホテル本線とは分離。

### Q71. 変数・出力の設計は？

**A.** `variables.tf` に ZIP パス・region 等。モジュール output で subnet_id 等を親に渡す。

### Q72. dummy zip（archive_file）の意味は？

**A.** Lambda ZIP が無くても `plan`/`validate` できるようにする**プレースホルダ**。

### Q73. 本番 apply で気をつけることは？

**A.** リモート state・ロック、ワークスペース、段階的 apply、rollback 手順、コスト見積もり。

### Q74. Terraform で苦労した点は？

**A.** （自分の体験）例: 循環参照（Sentinel と chatbot ログ）、module 化後の参照の張り替え。

### Q75. IAM はどう管理？

**A.** 各 Lambda / サービスごとにロール。最小権限を意識（コード上の policy を読んで説明）。

---

## 8. セキュリティ・コンプライアンス

### Q76. プロンプトインジェクション対策は？（★）

**A.** Type-B: Sentinel がログパターンで検知し封じ込め。Type-A: 書込を execution + スキーマに限定。

### Q77. 最小権限の例は？

**A.** reader は読取のみ、Sentinel は WAF/Cognito/DynamoDB の必要アクションのみ、など。

### Q78. 暗号化は？

**A.** KMS（S3、RDS、DynamoDB、ログ）、転送時 TLS。

### Q79. ログ・監査は？

**A.** CloudTrail、VPC Flow Logs、API GW アクセスログ、Lambda ログ。Athena サンプルは `AWS_log/`。

### Q80. データ主権（オフライン PCAP）とは？

**A.** ローカル LLM の Egress を**外部 API に頼らず** PCAP + GeoLite で分析（`analysis_wireshark/`）。

### Q81. 個人情報（PII）の扱いは？

**A.** ポートフォリオではマスキング本番実装まではしていない。本番ではログマスキング・保持期間・同意が必要、と付け加える。

### Q82. ゼロトラストとの関係は？

**A.** Type-A / 社内 Type-A は ID ベースアクセスの話と接続できる。外向けホテルは Cognito + API 認可。

### Q83. セキュリティであえてやっていないことは？

**A.** 本番 SOC 連携、24/7 オンコール、レッドチーム、PCI 完了、秘密ローテーション運用。

### Q84. WAF と Sentinel の役割分担は？

**A.** WAF はエッジでのフィルタ。**Sentinel はログ起因の動的ブロック**（IP セット更新）。

### Q85. 誤検知（False Positive）したら？

**A.** フィルタパターン調整、閾値、人手エスカレーション、release Lambda で解除。本番運用設計が必要。

---

## 9. AWS_log / analysis_wireshark

### Q86. AWS_log で何ができる？

**A.** CloudTrail / VPC Flow を **Athena SQL** で分析する例。誰が・いつ・何をしたか、REJECT フローなど。

### Q87. インフラ構築との違いは？

**A.** ログは既に（想定）溜まっている前提の**調査**スキル。Terraform とは補完関係。

### Q88. analysis_wireshark の用途は？

**A.** ローカル AI の**外向き通信**を PCAP から抽出し、オフラインで Geo/ASN 付与。

### Q89. なぜオフライン？

**A.** 調査データを外部 SaaS に送らない**データ主権**の原則を示すため。

### Q90. パケット解析の限界は？

**A.** 暗号化ペイロードの中身は見えない。メタデータ（IP/ポート/量）中心。

---

## 10. 弱点・トラップ・正直回答

### Q91. デモがないのは弱点では？（★）

**A.** 弱点にもなる。代わりに **IaC・テスト・ドキュメントで再現可能** と説明。入社後に検証環境を作る意欲を示す。

### Q92. 全部自分で理解していますか？

**A.** 全体像と主戦場は説明できる。Athena/PCAP/社内版の一部は**サンプルレベル**で深掘りはこれから、と正直に。

### Q93. Type-Omega / OpenClaw とは？

**A.** 旧名。**Type-A / Sentinel** にリネーム済み。

### Q94. コピー4 との違いは？

**A.** （知っていれば）フォルダ統合、Type-A 関所分離、Sentinel 命名、CI、Terraform 整理など。知らなければ「同一系統の改良版」。

### Q95. なぜ Stripe / OTA がない？

**A.** スコープ外。`REAL_WORLD_HOTEL_INTEGRATIONS.md` に本番で必要な要素として記載。

### Q96. AI に仕事を奪わない？

**A.** AI は UX と効率化。**確定処理・防御・監査**は人間が設計したルールと関所に残す、という立場。

### Q97. Bedrock 依存のベンダーロックインは？

**A.** リスクはある。緩和は abstraction、マルチモデル、Type-B のように VPC 内 LLM 案を併存、など。

### Q98. 在庫・ダブルブッキングは？

**A.** 冪等キーとスキーマはあるが、**在庫ロック・行レベル整合**まではポートフォリオ完備ではない。本番は DB 制約・分散ロックが必要。

### Q99. テストカバレッジは？

**A.** クリティカルパス中心。E2E・負荷・カオスは未実施と正直に。

### Q100. 使った AI ツールは？

**A.** （事実に合わせて）学習・下書きに使ったが、設計判断と検証は自分、など正直に。

---

## 11. 行動・新卒・カルチャー

### Q101. なぜインフラ / SRE / セキュリティ志望？

**A.** （自分用に書く）例: 設計の筋道とガードレールをコードで表すのが好き、障害を減らすのが面白い。

### Q102. チームで何をしたい？

**A.** 設計レビュー、IaC、監視、インシデント振り返りに貢献したい。

### Q103. 入社後に学びたいことは？

**A.** 本番運用、オンコール、大規模システムの変更管理、組織のセキュリティ基準。

### Q104. 失敗経験は？

**A.** （STAR で）例: スコープを広げすぎた → PORTFOLIO_STRATEGY で切った、など実話。

### Q105. コードレビューで指摘されたら？

**A.** 理由を聞き、直す。セキュリティ指摘は優先。

### Q106. 期限に間に合わないときは？

**A.** 早めに相談、スコープを切る、再現手順を残す。

### Q107. 新卒で不安な点は？

**A.** 本番運用経験はこれから。ポートフォリオで**学習の速さと設計の入口**は示した。

### Q108. なぜうちの会社？

**A.** （会社研究を入れる）事業・技術スタック・育成・クラウド方針との一致。

---

## 12. 逆質問（面接官に聞く）

### 技術

1. 新卒の Terraform / AWS 研修やメンター制度はありますか？  
2. 本番変更はどの程度 CI/CD とレビューが必須ですか？  
3. SRE とセキュリティチームの分担は？  
4. 生成 AI を業務で使う場合のガイドラインはありますか？  

### 成長

5. 入社1年目に期待される成果のイメージは？  
6. ポートフォリオのような個人開発を評価する観点はありますか？  

### 文化

7. オンコールや障害対応に新卒が関わるタイミングは？  
8. 設計ドキュメントはどの形式が主流ですか？  

---

## 13. STAR エピソード例（穴埋め用）

### エピソード1: スコープ管理

- **S:** ホテル予約ポートフォリオを作る際、機能を増やしすぎていた  
- **T:** 新卒のうちに「完成品」に見せるより設計比較を明確に  
- **A:** `PORTFOLIO_STRATEGY.md` で含む/含まないを定義。決済・デモ URL は除外  
- **R:** 説明可能な幅に収まり、`make ci` で品質の下限を担保  

### エピソード2: 関所の導入（Type-A）

- **S:** AI に予約を書かせると幻覚・二重予約のリスク  
- **T:** 対話は AI、確定はコードに分離したい  
- **A:** execution 層に AJV・冪等・pricing を実装しテスト  
- **R:** 設計意図を面接で図解して説明できるようになった  

### エピソード3: Terraform 整理

- **S:** main.tf が巨大で自分も追いにくかった  
- **T:** レビュー可能な単位に分割したい  
- **A:** ファイル分割 → vpc/sentinel モジュール化 → moved.tf  
- **R:** validate/plan が通り、変更箇所を説明しやすくなった  

---

## 14. デモ・手元操作（聞かれたら）

```bash
cd hotel_folder
make ci                    # テスト + terraform 検証
make terraform-plan-type-b # plan ファイル生成（要 AWS 認証ならより現実的）
```

言えること: 「CI で壊れにくくしている」「plan まで再現できる」。

---

## 15. 面接直前チェックリスト（★）

- [ ] 30秒自己紹介を声に出した  
- [ ] Type-B / Type-A を一言ずつ言える  
- [ ] Sentinel = ルール実行、と言える  
- [ ] 本番 SaaS ではない、と最初に言える  
- [ ] 主戦場を1つ決めた（コードの場所も）  
- [ ] `make ci` が通る状態  
- [ ] わからない質問の逃げ方を用意（「サンプルレベル」「これから学ぶ」）  
- [ ] 逆質問を2つ以上用意  

---

## 16. 超短答インデックス（30秒以内）

| 聞かれ方 | 超短答 |
|----------|--------|
| これ何？ | ホテル題材の Type-B/A 比較ポートフォリオ |
| 本番？ | いいえ。IaC+テストで検証 |
| Type-B？ | 3層+RDS、Sentinel が守る |
| Type-A？ | Agent+関所、書込は execution |
| Sentinel？ | ログ検知→WAF/Cognito。AIではない |
| なぜ2系統？ | ブランドで最適解が違うから |
| 得意は？ | （自分で1行） |
| 弱点は？ | デモ未ホスト、一部はサンプル |
| 新卒で凄い？ | 設計の幅は示せる。運用はこれから |

---

*志望企業名・自分の主戦場・共同制作の有無は、§1・§5・§11 の（　）を埋めてから本番に使ってください。*
