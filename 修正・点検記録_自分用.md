# 修正・点検記録（自分用）

ポートフォリオ全体に対するコードレビューと修正の記録。面接やセルフチェック用のメモ。

- 対象: `AWS_log/` / `analysis_wireshark/` / `hotel_folder/`（lambda・terraform・internal-infra・packages・Makefile 他）
- 検証: 全 Lambda テスト pass、`terraform validate` 全スタック Success、`terraform fmt` クリーン
- 補足: 残課題は「**実ドメイン + 本番デプロイ時のみ顕在化するもの**」だけ（後述）。`terraform validate` が通る現状のポートフォリオには影響しない。

---

## 0. サマリ

| 分類 | 件数の目安 | 代表例 |
|------|-----------|--------|
| 致命的（デプロイ/実行で動かない） | 5 | BedrockエイリアスID、VPCエンドポイントSG、KMS×SNS、IAM GetItem、空egress |
| 高（ビルド不整合・ドキュメント乖離） | 多数 | send_email未ビルド、スキーマ二重管理、Egressフィルタ未実装 |
| 中（セキュリティ・信頼性） | 多数 | 未使用IAM権限、TLS強制、サブネットCIDR変数化 |
| 軽微・衛生 | 多数 | .gitignoreタイポ、不審ファイル、未使用import |
| 保留（実ドメイン前提） | 4 | CloudFront/ALBのACM・オリジンTLS、RDSローテーション、Bedrock local-exec |

---

## 1. 致命的な修正（デプロイ/実行すると動かなかったもの）

### 1-1. Bedrock Agent のエイリアスID誤り
- **場所**: `hotel_folder/terraform/type-a/lambda.tf` / `variables.tf`
- **問題**: `BEDROCK_AGENT_ALIAS_ID` に変数デフォルト `"prod"`（エイリアス**名**）を渡していた。`InvokeAgent` にはAWS自動採番の**エイリアスID**が必要。
- **修正**: `aws_bedrockagent_agent_alias.prod.agent_alias_id` を直接参照。誤解を招く変数を削除。

### 1-2. Reservation Lambda の IAM に `dynamodb:GetItem` 欠如
- **場所**: `hotel_folder/terraform/type-a/lambda_iam.tf`
- **問題**: べき等性チェックで `GetCommand` を使うのに、IAMが Put/Update/Delete のみ → `AccessDeniedException`。
- **修正**: reservation ポリシーに `dynamodb:GetItem` を追加。

### 1-3. インターフェース型VPCエンドポイントにインバウンド443が無い
- **場所**: `hotel_folder/terraform/modules/vpc_type_a/main.tf` / `terraform/type-b/security_groups.tf`
- **問題**: エンドポイント用SGが egress のみ。Lambda/EC2 が Bedrock・KMS・Logs・Cognito・SecretsManager・SQS の各エンドポイントに到達できず、閉域構成が実質全滅。
- **修正**: エンドポイント用SGに `ingress 443 (VPC CIDR)` を追加。

### 1-4. KMS暗号化SNSにCloudWatchアラームが発行できない
- **場所**: `hotel_folder/terraform/type-b/kms.tf`
- **問題**: `ops_alert` トピックがKMS暗号化＋4アラームが発行するのに、キーポリシーに `cloudwatch.amazonaws.com` 許可が無く、通知が全てKMS拒否で失敗。
- **修正**: type-a と同じ `AllowEventsAndAlarmsToEncryptSNS`（events + cloudwatch）を追加。

### 1-5. DBセキュリティグループの空egress
- **場所**: `hotel_folder/terraform/type-b/security_groups.tf`
- **問題**: `egress { cidr_blocks = [] }` はapply時エラー。
- **修正**: 「全egress拒否」はegressブロックを書かないのが正解 → 削除（DLP意図はコメントで明記）。

---

## 2. 高優先（ビルド不整合・ドキュメント乖離）

### 2-1. `send_email` Lambda が未ビルド・未テスト
- **場所**: `hotel_folder/Makefile` / `lambda/type-b/send_email/package.json`
- **問題**: Terraform が `send_email.zip` を期待するのに、Makefile に build/test ターゲットなし。package.json に test スクリプト・vitest 欠如。
- **修正**: `build-send-email` / `test-send-email` を追加し全体ターゲットに組込み。package.json に test スクリプト + vitest を追加。

### 2-2. `intent.schema.json` の二重管理（ドリフト）
- **場所**: `hotel_folder/packages/reservation-schema/` と `lambda/type-a/execution/reservation/`
- **問題**: 2か所に別内容のスキーマ。STRUCTURE.md は packages 正典と言うのに、コードはローカルコピーを使用。
- **修正**: packages を正典に統合し、Makefile に `sync-schema` を追加して test/build 前に自動同期。

### 2-3. `pricing.js` の二重管理
- **場所**: `lambda/type-a/execution/reservation/pricing.js` ≒ `packages/pricing-engine/calculate.js`
- **修正**: `sync-schema` ターゲットを拡張し pricing も packages から自動同期。

### 2-4. Wireshark の Egress フィルタ未実装
- **場所**: `analysis_wireshark/analyze_ai_traffic.py`
- **問題**: README は「プライベート→パブリックのEgressのみ抽出」と謳うが、実際は全トラフィックを集計。「0件＝安全」の根拠が崩れていた。
- **修正**: `is_egress()`（src=private かつ dst=global）を実装しフィルタ。出力にEgressパケット数・0件時メッセージを追加。READMEの出力例もCSV列に整合。

### 2-5. `setup.sh` が壊れていた
- **場所**: `hotel_folder/setup.sh`
- **問題**: 未終端ヒアドキュメント、古いパス `lambda/type-a/reader`、現行docsを古いスタブで上書きする危険。
- **修正**: 既存ファイルを上書きしない「構成確認スクリプト」に置換（正しいパスのみ作成）。

---

## 3. 中優先（セキュリティ・信頼性）

### 3-1. 予約のべき等性レコード孤児化（実害バグ）
- **場所**: `lambda/type-a/execution/reservation/index.js`
- **問題**: べき等キー → 予約本体の順で別々に書込み。予約本体の書込み失敗時に「予約が無いのにべき等キーだけ残る」状態になり、リトライで存在しない `reservationId` を返す。
- **修正**: `TransactWriteCommand` で両方を単一トランザクションで原子的に書込み。競合時は既存予約を返す。テスト2件追加（7→9件）。

### 3-2. JSON.parse の未保護
- **場所**: `lambda/type-a/interface/index.js` / `internal-infra/lambda/rag_query/index.js`
- **問題**: 不正JSONボディで500になる（400であるべき）。rag_query は数値クエリで `TypeError`。
- **修正**: try/catch で400、`String()` 正規化。各テスト追加。

### 3-3. severity の型安全化
- **場所**: `internal-infra/lambda/sentinel_enterprise/main.py`
- **問題**: Security Hub/GuardDuty は severity を数値や dict で返すことがあり `.upper()` でクラッシュ。
- **修正**: `_normalize_severity()` で文字列/数値/`{Label,Normalized}` を正規化。proxy互換のため body を JSON文字列化（sentinel_cloud_native も同様）。

### 3-4. 未使用IAM権限の削除 / 未使用設定の整理
- type-a reader の未使用 `dynamodb:Scan` を削除（`terraform/type-a/lambda_iam.tf`）
- send_email の未使用 `SECRET_ARN` env と `secretsmanager:GetSecretValue` を削除（`terraform/type-b/messaging.tf`）。KMS復号はSQS用に保持。

### 3-5. サブネットCIDRの変数化 + DynamoDBエンドポイント
- `internal-infra/terraform/type-{a,b}`: ハードコードCIDRを `cidrsubnet(var.vpc_cidr, ...)` 化。
- `internal-infra/terraform/type-a`: 会話履歴テーブル用に DynamoDB ゲートウェイエンドポイントを追加。

### 3-6. S3 TLS強制ポリシー（転送時暗号化）
- `internal-infra/terraform/type-b/foundations.tf`（コンプライアンスバケット）: バージョニング + `aws:SecureTransport=false` Deny を追加。
- `terraform/corp-internal/s3_internal.tf`（成果物バケット）: TLS強制 Deny を追加（一貫性）。

### 3-7. `required_version` の不整合
- `terraform/corp-internal` / `type-a` / `type-b` / `internal-infra/terraform/type-{a,b}`
- `use_lockfile`(TF1.10+) や `moved`ブロック(1.1+) を使うのに `>= 1.0.0` 宣言 → `>= 1.10.0` に統一。

### 3-8. Dependabot 設定の網羅
- **場所**: ルート `.github/dependabot.yml`（GitHubが読むのはリポジトリ直下のみ）
- **問題**: send_email・pip・internal-infra のエントリが欠落し、更新監視が空振り。
- **修正**: 全 Lambda（npm/pip）を網羅。nested `hotel_folder/.github/dependabot.yml` にも send_email を追加。

---

## 4. 軽微・衛生

- `.gitignore` のタイポ `terraform:/type-*` → `terraform/type-*` 修正（`hotel_folder/.gitignore`）。
- `Makefile clean` の存在しない `lambda/writer.zip` 参照を削除。
- 未使用 `import json` 削除、テストの追加・整理。
- 不審ファイル削除: `terraform/type-a/Claude.pdf`（無関係な2.7MB PDF）、`terraform/type-a/plan.tfplan`、`terraform/type-b/plan.tfplan`。
- `analysis_wireshark/requirements.txt` 追加（pyshark / geoip2 / mac-vendor-lookup）。README にオフライン時のMAC DB挙動・GeoLite配置を明記。
- internal-infra README に実装状況の注記（`provisioner` Lambda 未実装、Sentinelフローの実挙動）。`type-b/outputs.tf` の誤った説明（flow log storage）を修正。internal-infra Makefile の "build" 文言を是正。

---

## 5. CI が緑でも防げなかった理由（重要メモ）

GitHub CI は次しか実行しない:
- `make test`（Lambdaのユニットテスト＝ローカル/モック）
- `terraform fmt -check`（整形確認）
- `terraform validate`（構文・参照チェック。AWSに接続しない）

→ **「IAM権限不足」「環境変数の値の誤り」「SGのingress漏れ」「KMS×SNSの許可漏れ」などのランタイム/AWSセマンティクスはCIではすり抜ける。** 上記1章の致命的な多くは、AWSへ実apply/実行して初めて顕在化するもの。

---

## 6. 検証結果

- **Lambda テスト**: 全 pass
  - Type-B: chatbot / send_email / sentinel_executioner / sentinel_release
  - Type-A: interface(6) / tools-reader / execution-reservation(9)
  - internal-infra: rag_query(5) / sentinel_enterprise(6) / sentinel_cloud_native(4)
- **Terraform validate**: type-a / type-b / corp-internal / internal-infra type-a / type-b すべて Success
- **Terraform fmt**: クリーン
- **Python**: `analyze_ai_traffic.py` 構文OK + Egressロジック動作確認

---

## 7. 保留（実ドメイン + 本番デプロイ時にまとめて対応）

ドメイン未取得のため、現段階では対応不要（`terraform validate` には影響しない）。

- **type-b CloudFront → ALB のオリジンTLSホスト名不一致**（origin=ALB DNS名 vs 証明書=独自ドメイン）。`http-only` オリジン化 or 証明書設計の選択が必要。
- **type-b ACM 証明書の検証 `aws_acm_certificate_validation` がコメントアウト**。CloudFront/ALB とも独自ドメイン + DNS検証が前提。
- **RDS ローテーション（SAR）の `secretArn` 未指定 + ローテーションLambdaのDB(3306) egress 無し**。
- **Bedrock `prepare-agent` を `local-exec` で実行**（CI/CDでapplyしづらい）。

> いずれも `docs/PORTFOLIO_STRATEGY.md` の Future Works（本番化フェーズ）と整合。

---

## 8. 今後のための再発防止メモ

- 共有ロジック（スキーマ/料金）は **Source of Truth を packages に固定し、`make sync-schema` で同期**。手コピー禁止。
- 新しい Lambda を足したら **Makefile（build/test）・Dependabot（ルート）・Terraform（zip path）** の3点セットを必ず更新。
- KMS暗号化リソース（SNS/SQS/Logs/S3）を足したら **鍵ポリシーに使用サービスの許可**を必ず追加。
- VPC内リソースを足したら **エンドポイント用SGの ingress 443** を確認。
- Terraform の新機能（`use_lockfile` 等）を使ったら **`required_version`** を合わせる。
